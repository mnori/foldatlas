<VirtualHost *:80>
        ServerName rnabrowser.dev

        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined

        # This points Apache towards the development server, which you can reach by running
        # rn-runDevServer inside vagrant.

        ProxyPass / http://localhost:5000/
        ProxyPassReverse / http://localhost:5000/

</VirtualHost>

<VirtualHost *:80>
        ServerName static.rnabrowser.dev
        DocumentRoot "/var/www/static"
        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined
        Options FollowSymLinks
</VirtualHost>

<VirtualHost *:80>
        ServerName pma.rnabrowser.dev
        DocumentRoot "/usr/share/phpmyadmin"
        ErrorLog ${APACHE_LOG_DIR}/error.log
        CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>

# vim: sntax=apache ts=4 sw=4 sts=4 sr noet
